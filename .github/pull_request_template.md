Closes #

## Summary

<!-- What does this PR do and why? -->

## Stacked on

<!-- Optional: link the PR this one is stacked on top of, if any -->

## Checklist

- [ ] Tests added/updated for the new/changed behavior
- [ ] `uv run ruff check` passes
- [ ] `uv run ruff format --check` passes
- [ ] `uv run lint-imports` passes (no port-boundary violations)
- [ ] `uv run pytest` passes
- [ ] `README.md` / `README_DEV.md` updated if user-facing behavior or architecture changed
- [ ] `.env.example` updated if new settings were added
- [ ] No secrets, tokens, or `.env` files committed
